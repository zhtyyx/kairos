# 贡献说明

1. 使用 Node.js 22.12+，运行 `npm ci`。
2. 修改源码，新增涉及权限或用户数据的行为时补充有效测试。
3. 运行构建与隐私测试、相关 UI 测试及发布扫描。
4. 检查 `git diff --cached`，确保没有凭据、个人信息或生成产物，再提交 PR。

新代码沿用 Apache-2.0。引入第三方代码、图片或字体时保留来源和许可证，避免复制无法确认授权的素材。不要在 PR、截图或测试输出中使用真实用户数据。

## 发布

运行 README 中的检查，并用 `python3 scripts/check-release.py --history` 扫描当前分支历史。曾提交过的密钥必须在提供方轮换。

发布前确认提交身份、代码和素材授权，确保发布范围内的全部 Git 历史通过检查。
