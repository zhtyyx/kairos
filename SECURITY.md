# 安全说明

不要在公开 issue 中粘贴密钥、访问令牌、个人数据库或未脱敏的截图。优先使用 GitHub 私密漏洞报告；若仓库尚未启用此功能，请先仅报告需要建立私密沟通渠道，不公开漏洞细节或凭据。

- 本地模式数据在 IndexedDB；浏览器 AI key 在 localStorage。清理站点数据前先导出，公共设备不保存 key。
- 不应将真实部署配置、日志、测试录像、代理记忆文件或历史凭据加入 Git。

提交前运行 `npm run check:release`。它是针对常见泄露的辅助检查，不能取代代码审查。密钥一旦进入 Git，应在提供方撤销/轮换；删除文件或重写历史不会使密钥失效。
